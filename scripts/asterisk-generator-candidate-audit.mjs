import fs from 'node:fs';
import path from 'node:path';
import vm from 'node:vm';
import {performance} from 'node:perf_hooks';
import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),index=fs.readFileSync(path.join(root,'index.html'),'utf8');
const refs=[...index.matchAll(/<script src="([^"]+)"><\/script>/g)].map(m=>m[1]);
const wanted=refs.filter(x=>/^games\/sudoku-bank(?:-iteration\d+)?\.js$/.test(x)||['games/sudoku-generator.js','games/extra-house-generator-core.js','games/iteration4-generator-hardening.js'].includes(x));
const ctx={console,globalThis:null,window:null,Map,Set,WeakMap};ctx.globalThis=ctx;ctx.window=ctx;vm.createContext(ctx);for(const ref of wanted)vm.runInContext(fs.readFileSync(path.join(root,ref),'utf8'),ctx,{filename:ref});vm.runInContext(fs.readFileSync(path.join(root,'games/asterisk-generator-hardening.js'),'utf8'),ctx,{filename:'games/asterisk-generator-hardening.js'});
const G=ctx.SudokuGenerator,v=ctx.SudokuBank.find(x=>x.id==='asterisk');if(!v)throw new Error('missing asterisk');
const diffs=['gentle','focused','expert'],seeds=Array.from({length:32},(_,i)=>41000+i),sols=new Set(),structs=new Set(),tops=new Set(),puzzles=new Set();let valid=0,unique=0,essential=0,det=0,fail=0;const rows={};
function norm(g){const m=new Map();let n=1;return g.flat().map(x=>{if(!m.has(x))m.set(x,n++);return m.get(x)}).join(',')}
function cellsValid(g){const cells=g.data.cells||[];if(cells.length!==9)return false;const rs=new Set(),cs=new Set(),bs=new Set(),ds=new Set();for(const [r,c] of cells){rs.add(r);cs.add(c);bs.add(Math.floor(r/3)*3+Math.floor(c/3));ds.add(g.solution[r][c]);}return rs.size===9&&cs.size===9&&bs.size===9&&ds.size===9;}
function pct(a,p){a=a.slice().sort((x,y)=>x-y);return a[Math.ceil(a.length*p)-1]}
for(const d of diffs){const times=[],clues=[];for(const seed of seeds){try{const t=performance.now(),g=G.make(v,seed,d);times.push(performance.now()-t);const r=G.make(v,seed,d);if(JSON.stringify(g)===JSON.stringify(r))det++;else fail++;if(cellsValid(g))valid++;else fail++;if(G.countVariantSolutions(g.puzzle,g,2)===1)unique++;else fail++;if(G.countSolutions(g.puzzle,2)>1&&g.generation?.variantEssential===true)essential++;else fail++;sols.add(JSON.stringify(g.solution));structs.add(norm(g.solution));tops.add(JSON.stringify(g.data.cells));puzzles.add(JSON.stringify(g.puzzle));clues.push(g.puzzle.flat().filter(Boolean).length);}catch(e){fail++;}}rows[d]={samples:times.length,clueMin:Math.min(...clues),clueMax:Math.max(...clues),runtimeP50Ms:+pct(times,.5).toFixed(1),runtimeP95Ms:+pct(times,.95).toFixed(1),runtimeMaxMs:+Math.max(...times).toFixed(1)};}
const total=diffs.length*seeds.length,result={samples:total,solutionUnique:sols.size,structuralSolutionUnique:structs.size,topologyUnique:tops.size,puzzleUnique:puzzles.size,valid,exactUnique:unique,variantEssential:essential,deterministic:det,failures:fail,difficulties:rows};
const correctness=valid===total&&unique===total&&essential===total&&det===total&&fail===0,diversity=sols.size>=90&&structs.size>=80&&tops.size>=80&&puzzles.size>=90,runtime=diffs.every(d=>rows[d].samples===seeds.length&&rows[d].runtimeP95Ms<500&&rows[d].runtimeMaxMs<1000),ordering=rows.gentle.clueMin>rows.focused.clueMax&&rows.focused.clueMin>rows.expert.clueMax;
console.log('ASTERISK_CANDIDATE_AUDIT '+JSON.stringify(result));console.log('ASTERISK_CANDIDATE_CORRECTNESS:'+(correctness?'PASS':'FAIL'));console.log('ASTERISK_CANDIDATE_DIVERSITY:'+(diversity?'PASS':'FAIL'));console.log('ASTERISK_CANDIDATE_RUNTIME:'+(runtime?'PASS':'FAIL'));console.log('ASTERISK_CANDIDATE_DIFFICULTY_ORDERING:'+(ordering?'PASS':'FAIL'));console.log('ASTERISK_CANDIDATE_GATE:'+(correctness&&diversity&&runtime&&ordering?'PASS':'FAIL'));if(!(correctness&&diversity&&runtime&&ordering))process.exitCode=1;
